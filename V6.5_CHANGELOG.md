# MediCard FastLearn V6.5 — 核心算法与医学场景深化重构

**发布日期**: 2026-05-25  
**基于版本**: V6.0 (FastLearn 空间重复系统)

---

## 概述

V6.5 在 V6.0 双轨记忆、交错调度、考前模式基础架构之上，完成了 7 项核心升级：

1. 激活 3 个已实现但未加载的 cramming 模块
2. 漏洞修复从"仅标记"升级为自动执行
3. 错误基因从 5 种通用分类扩展为 8 种（含 3 种医学专用）
4. 新增知识前置依赖检查框架（优雅降级）
5. 新增病例组处理框架（A3/A4 型题支持）
6. 持久化层加固（分片存储 + IndexedDB 降级 + 导入导出）
7. 考前冲刺模式 UI 入口（倒计时、模式切换、冲刺报告）

---

## 新增文件

| 文件 | 行数 | 说明 |
|------|------|------|
| `src/modules/fastlearn/fastlearn-storage.js` | ~319 | 持久化适配器：localStorage 分片 + IndexedDB 降级 + .medicard 导入导出 |
| `src/modules/fastlearn/fastlearn-prereq.js` | ~206 | 知识前置依赖框架：掌握度检查、薄弱点扫描、补漏题筛选 |
| `src/modules/fastlearn/fastlearn-casegroup.js` | ~165 | 病例组处理框架：A3/A4 题型原子单元、共享题干判断 |

---

## 修改文件

### `index.html`
- 新增 5 个 `<script>` 标签（按依赖顺序）：
  - `fastlearn-storage.js` — 持久化（在 core 之前加载）
  - `fastlearn-prereq.js` — 前置依赖框架（在 core 之后加载）
  - `fastlearn-casegroup.js` — 病例组框架
  - 原有 3 个 cramming 模块（`dual-track-memory.js`, `interleaving-scheduler.js`, `fastlearn-cramming.js`）从死代码激活

### `tools/build-prod.js`
- JS_FILES 数组新增 4 项：`fastlearn-storage.js`, `fastlearn-prereq.js`, `fastlearn-casegroup.js`, `fastlearn-cramming.js`
- 总打包文件数：53 → 57

### `src/modules/fastlearn/fastlearn-core.js`
- **漏洞修复自动执行**：
  - `recordSessionAnswer()` 中自动调用 `buildVulnerabilityFix()`，将 basic 题插入 `currentIndex + 1`
  - 新增 `_vulnFixActive` 标志，补漏期间暂停记忆等级更新
  - `advanceQuestion()` 管理补漏生命周期（basic → advanced → confusing → 结束）
- **错误基因扩展**（5 → 8 种）：
  - 新增 `differential_confusion`（鉴别混淆）— 同科目不同疾病
  - 新增 `dose_numeric_error`（数值错误）— 提取数字比较数量级
  - 新增 `indication_contra`（适应症错误）— 药物/操作场景不匹配
- **持久化委托**：`save()`, `load()`, `exportData()`, `importData()` 委托给 `MediCard.FastLearnStorage`

### `src/modules/ui-system/screen-fastlearn.js`
- **考前模式 UI**：
  - 仪表盘新增「考前冲刺」区域（开关、倒计时、<24h 冻结提示）
  - 题目卡片右上角倒计时徽章 + 记忆等级徽章 `.cram` 样式
  - 开始学习路由到 `MediCard.FastLearnCramming.initSession()`
  - 答题通过 `MediCard.FastLearnCramming.recordSessionAnswer()`
  - 考前报告生成器
- **漏洞修复提示**：`_showVulnFixToast(kp)` — 2 秒自动消失的橙色 toast
- **错误基因针对性提示**：8 种基因每种一行医学建议
- **干扰项深度解析**：可折叠的 `.distractor-analysis` 面板（`whyWrong`、`commonMistake`、`trapType`）

### `src/css/screens/screen-fastlearn.css`
- 新增 ~120 行样式：
  - `.fl-gene-hint` — 医学提示框（紫色左边框）
  - `.distractor-analysis` 系列 — 折叠陷阱分析面板
  - `.fl-vuln-toast` — 固定定位动画 toast
  - `.fl-cram-section` 系列 — 考前模式仪表盘区域
  - `.fl-cram-badge` — 倒计时徽章（红橙渐变）
  - `.fl-memory-badge.cram` — 橙色冲刺记忆徽章
  - 全部含 `@media (max-width: 480px)` 响应式适配

---

## 设计原则

- **优雅降级**：所有新元数据字段（`prerequisites`, `questionGroup`, `distractorAnalysis`, `illnessScript`）当前题库均不存在，框架以休眠状态等待元数据就绪
- **模块化隔离**：每个框架模块独立 IIFE，通过 `window.MediCard` 命名空间暴露
- **向后兼容**：V6.0 所有功能保持不变，新增逻辑仅在条件满足时激活
- **不生成虚构数据**：未修改题库文件，所有题目保持医学教科书原貌

---

## 验证结果

- ✅ 所有 7 个 FastLearn 模块通过 `node --check` 语法检查
- ✅ `node tools/build-prod.js` 构建成功（57/57 JS 文件，23/23 CSS 文件）
- ✅ 加载顺序正确：storage → core → prereq → casegroup → dual-track → interleaving → cramming → screen

---

## 文件统计

| 类别 | 文件数 | 总行数 |
|------|--------|--------|
| 新增模块 | 3 | ~690 |
| 修改模块 | 2 | ~350 行变更 |
| CSS 新增 | 1 | ~120 行 |
| 配置修改 | 2 | ~8 行 |
| **总计** | **8** | **~1168** |
